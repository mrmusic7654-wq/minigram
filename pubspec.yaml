name: minigram
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  
  # FIXED VERSION - Resolves the conflict
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0
  
  # FIXED VERSION - Compatible with json_annotation 4.9.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
